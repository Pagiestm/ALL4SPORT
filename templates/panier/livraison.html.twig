{% extends 'base.html.twig' %}

{% block title %}Livraison{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="span12">
            <h2>Livraison</h2>
            <div id="collapseOne" class="accordion-body collapse in">
                <div class="accordion-inner">
                    <div class="span4">
                        <form action="{{ path ('app_commande') }}">
                            <h4>Adresse de livraison</h4>
                            {% for utilisateurAdresses in utilisateurAdresses %}
                            <label class="radio">
                                <input type="radio" name="optionsRadios" id="optionsRadios1" value="option1" checked="">
                                {{ utilisateurAdresses.adresse }}, {{ utilisateurAdresses.cp }} {{ utilisateurAdresses.ville }}, {{ utilisateurAdresses.Pays }} 
                                <a href="#"><i class="icon-trash"></i></a>
                            </label>
                            {% endfor %}
                            <br /><br />
                            <button class="btn btn-primary">Valider mon adresse de livraison + ma commande </button>
                        </form>
                    </div>
				<div class="span4 offset2">
					<h4>Ajouter une nouvelle adresse</h4>
                    {{ form(utilisateurAdresse) }}
					</div>
				</div>
			</div>					
	</div>
</div>
{% endblock %}